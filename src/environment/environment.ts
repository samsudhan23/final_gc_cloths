export const enviornment = {
    // Local API — use while testing Cloudinary uploads (npm run dev in clono_api/node_ec)
    // url: "http://localhost:5000/api/",
    // Production API (Render) — deploy latest node_ec code + set CLOUD_NAME, API_KEY, API_SECRET first
    url: "https://node-ec-1.onrender.com/api/",
    production: false
}